import React from 'react';
export default function Badge({text, color='blue'}) { return <span className={px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg--100 text--800}>{text}</span>; }
